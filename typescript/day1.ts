// 문제1: 아래의 코드에서 변수 myName과 message의 타입을 명시적으로 지정하세요.
const myName:string = "Steve";
const message:string = `Hello, ${myName}`;
// 문제2: 아래의 코드에서 변수 n의 타입을 명시적으로 지정하세요.
const n:number = 123;
// 문제3: 아래의 코드에서 변수 isOpen과 isCompleted의 타입을 명시적으로 지정하세요.
let isOpen:boolean = true;
let isCompleted:boolean = false;
// 문제4: 아래의 코드에서 변수 user의 타입을 명시적으로 지정하세요.
let user:string|null = null;
// 문제5: 아래의 코드에서 변수 someValue의 타입을 명시적으로 지정하세요.
let someValue:string|boolean;
someValue = false;
someValue = "Hello";